<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - <%= siteName %></title>
    
    <!-- Primary Meta Tags -->
    <meta name="description" content="<%= description %>">
    <meta name="author" content="<%= post.author %>">
    <meta name="keywords" content="<%= post.tags ? post.tags.join(', ') : '' %>">
    <meta name="robots" content="index, follow, max-image-preview:large">
    <meta name="theme-color" content="#667eea">

    <!-- Canonical URL -->
    <link rel="canonical" href="<%= url %>">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="article">
    <meta property="og:site_name" content="<%= siteName %>">
    <meta property="og:locale" content="en_US">
    <meta property="og:url" content="<%= url %>">
    <meta property="og:title" content="<%= title %>">
    <meta property="og:description" content="<%= description %>">
    <meta property="og:image" content="<%= image %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:image:alt" content="<%= title %>">
    <meta property="article:published_time" content="<%= new Date(post.publishedAt).toISOString() %>">
    <meta property="article:modified_time" content="<%= new Date(post.updatedAt).toISOString() %>">
    <meta property="article:author" content="<%= post.author %>">
    <meta property="article:section" content="<%= post.category %>">
    <% if (post.tags) { %>
        <% post.tags.forEach(tag => { %>
            <meta property="article:tag" content="<%= tag %>">
        <% }); %>
    <% } %>

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:site" content="<%= twitterHandle %>">
    <meta name="twitter:creator" content="<%= twitterHandle %>">
    <meta name="twitter:url" content="<%= url %>">
    <meta name="twitter:title" content="<%= title %>">
    <meta name="twitter:description" content="<%= description %>">
    <meta name="twitter:image" content="<%= image %>">
    <meta name="twitter:image:alt" content="<%= title %>">
    <meta name="twitter:label1" content="Written by">
    <meta name="twitter:data1" content="<%= post.author %>">
    <meta name="twitter:label2" content="Est. reading time">
    <meta name="twitter:data2" content="<%= post.readTime || 5 %> minutes">

    <!-- Additional Social Meta -->
    <meta property="fb:app_id" content="<%= fbAppId %>">
    <meta name="pinterest" content="nopin" description="Sorry, you cannot pin from our site">

    <!-- Structured data (JSON-LD) -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "BlogPosting",
        "mainEntityOfPage": {
            "@type": "WebPage",
            "@id": "<%= url %>"
        },
        "headline": "<%= title %>",
        "description": "<%= description %>",
        "image": {
            "@type": "ImageObject",
            "url": "<%= image %>",
            "width": "1200",
            "height": "630"
        },
        "author": {
            "@type": "Person",
            "name": "<%= post.author %>",
            "url": "<%= siteUrl %>/author/<%= post.author.toLowerCase().replace(/\\s+/g, '-') %>"
        },
        "publisher": {
            "@type": "Organization",
            "name": "<%= siteName %>",
            "logo": {
                "@type": "ImageObject",
                "url": "<%= siteUrl %>/logo.png",
                "width": "180",
                "height": "60"
            },
            "url": "<%= siteUrl %>",
            "sameAs": [
                "<%= siteUrl %>",
                "https://twitter.com/<%= twitterHandle.replace('@', '') %>"
            ]
        },
        "datePublished": "<%= new Date(post.publishedAt).toISOString() %>",
        "dateModified": "<%= new Date(post.updatedAt).toISOString() %>",
        "wordCount": "<%= post.content ? post.content.split(' ').length : 0 %>",
        "timeRequired": "PT<%= post.readTime || 5 %>M",
        "articleSection": "<%= post.category %>",
        "articleBody": "<%= post.content ? post.content.replace(/<[^>]*>/g, '').substring(0, 500) : '' %>",
        "keywords": "<%= post.tags ? post.tags.join(', ') : '' %>",
        "inLanguage": "en-US",
        "copyrightYear": "<%= new Date(post.publishedAt).getFullYear() %>",
        "copyrightHolder": {
            "@type": "Organization",
            "name": "<%= siteName %>"
        },
        "discussionUrl": "<%= url %>#comments"
    }
    </script>

    <!-- Additional Schema for Breadcrumb -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "BreadcrumbList",
        "itemListElement": [
            {
                "@type": "ListItem",
                "position": 1,
                "name": "Home",
                "item": "<%= siteUrl %>"
            },
            {
                "@type": "ListItem",
                "position": 2,
                "name": "Blog",
                "item": "<%= siteUrl %>/blog"
            },
            {
                "@type": "ListItem",
                "position": 3,
                "name": "<%= post.category %>",
                "item": "<%= siteUrl %>/category/<%= post.category.toLowerCase() %>"
            },
            {
                "@type": "ListItem",
                "position": 4,
                "name": "<%= title %>",
                "item": "<%= url %>"
            }
        ]
    }
    </script>

    <!-- Favicon and App Icons -->
    <link rel="icon" type="image/x-icon" href="<%= siteUrl %>/favicon.ico">
    <link rel="apple-touch-icon" sizes="180x180" href="<%= siteUrl %>/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="<%= siteUrl %>/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="<%= siteUrl %>/favicon-16x16.png">
    <link rel="manifest" href="<%= siteUrl %>/site.webmanifest">

    <!-- AMP Alternative -->
    <link rel="amphtml" href="<%= siteUrl %>/amp/<%= post.slug %>">

    <!-- RSS Feed -->
    <link rel="alternate" type="application/rss+xml" title="RSS Feed for <%= siteName %>" href="<%= siteUrl %>/rss.xml">

    <!-- Preload Critical Resources -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="preload" as="image" href="<%= image %>">

    <!-- Security Headers (would be set in server, but included here for reference) -->
    <meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self' 'unsafe-inline' https://cdn.ampproject.org; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: https:; font-src 'self' https://fonts.gstatic.com;">

    <style>
        /* Basic styles for the preview */
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            line-height: 1.6;
            margin: 0;
            padding: 20px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }
        .container {
            max-width: 800px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            padding: 3rem;
            border-radius: 20px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }
        h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            font-weight: 800;
        }
        .description {
            font-size: 1.2rem;
            margin-bottom: 2rem;
            opacity: 0.9;
        }
        .meta {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }
        .meta-item {
            background: rgba(255, 255, 255, 0.2);
            padding: 0.5rem 1rem;
            border-radius: 25px;
            font-size: 0.9rem;
        }
        .featured-image {
            max-width: 100%;
            height: auto;
            border-radius: 12px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            margin: 2rem 0;
        }
        .site-info {
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.3);
            width: 100%;
        }
        .btn {
            display: inline-block;
            background: white;
            color: #667eea;
            padding: 12px 24px;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            margin-top: 1rem;
            transition: transform 0.2s ease;
        }
        .btn:hover {
            transform: translateY(-2px);
        }
        .tags {
            display: flex;
            flex-wrap: wrap;
            gap: 0.5rem;
            justify-content: center;
            margin: 1rem 0;
        }
        .tag {
            background: rgba(255, 255, 255, 0.2);
            padding: 0.3rem 0.8rem;
            border-radius: 15px;
            font-size: 0.8rem;
        }
        @media (max-width: 768px) {
            .container {
                padding: 2rem 1rem;
                margin: 1rem;
            }
            h1 {
                font-size: 2rem;
            }
            .meta {
                gap: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Site Branding -->
        <div style="margin-bottom: 2rem;">
            <h2 style="margin: 0; font-size: 1.5rem; opacity: 0.8;"><%= siteName %></h2>
            <p style="margin: 0.5rem 0 0 0; opacity: 0.6;">Premium Technology Content</p>
        </div>

        <!-- Main Content -->
        <h1><%= title %></h1>
        
        <p class="description"><%= description %></p>

        <!-- Meta Information -->
        <div class="meta">
            <div class="meta-item">👤 <%= post.author %></div>
            <div class="meta-item">📅 <%= new Date(post.publishedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></div>
            <div class="meta-item">⏱️ <%= post.readTime || 5 %> min read</div>
            <div class="meta-item">📊 <%= post.views || 0 %> views</div>
        </div>

        <!-- Category and Tags -->
        <% if (post.category) { %>
            <div class="meta-item" style="background: rgba(255, 255, 255, 0.3);">
                📁 <%= post.category %>
            </div>
        <% } %>

        <% if (post.tags && post.tags.length > 0) { %>
            <div class="tags">
                <% post.tags.forEach(tag => { %>
                    <span class="tag">#<%= tag %></span>
                <% }); %>
            </div>
        <% } %>

        <!-- Featured Image -->
        <img src="<%= image %>" alt="<%= title %>" class="featured-image" onerror="this.style.display='none'">

        <!-- Call to Action -->
        <div style="margin-top: 2rem;">
            <p style="margin-bottom: 1rem; opacity: 0.8;">Ready to dive into this article?</p>
            <a href="<%= url %>" class="btn">📖 Read Full Article</a>
        </div>

        <!-- Site Information -->
        <div class="site-info">
            <p style="margin: 0; opacity: 0.7;">
                &copy; <%= new Date().getFullYear() %> <%= siteName %> • 
                <a href="<%= siteUrl %>" style="color: white; text-decoration: underline;">Visit Our Blog</a>
            </p>
            <p style="margin: 0.5rem 0 0 0; font-size: 0.9rem; opacity: 0.6;">
                This preview is generated for social media and search engines.
            </p>
        </div>
    </div>

    <!-- Simple Analytics Script (Non-blocking) -->
    <script>
        // Track social preview views
        window.addEventListener('load', function() {
            if (navigator.sendBeacon) {
                const data = new FormData();
                data.append('type', 'social_preview');
                data.append('slug', '<%= post.slug %>');
                data.append('referrer', document.referrer);
                navigator.sendBeacon('<%= siteUrl %>/api/analytics/preview', data);
            }
        });
    </script>
</body>
</html>